# Flagship 360° panorama (Phase 6)

Replace `flagship-360.jpg` with your real equirectangular (360°) photo of the flagship site.

- Current flagship POI id: `ouzoud` (change in `src/data/flagshipPano.js` if needed)
- Format: equirectangular JPG/PNG
- Tip: phone panorama / 360 mode, then export as a single wide image

The file currently in this folder is a **temporary sample** from pannellum.org so the viewer works before your photo is ready.
